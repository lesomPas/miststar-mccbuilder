[project]
name = "miststar-serializer"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "tomlkit>=0.13.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = [
    "serializer/",
]